{
  "repo": {
    "uri": "{{.RepoURI}}",
    "org": "local",
    "repo": "test-repo",
    "defaultBranch": "main"
  },
  "branches": {
    "main": {
      "exists": true,
      "path": "{{.WorktreePath}}",
      "lastSync": "{{.LastSync}}"
    }
  }
}
