Print a markdown preamble fragment for embedding in skill files.
Agents read this to know how to self-upgrade git-hop before executing tasks.

Usage:
  git-hop upgrade preamble [flags]

Flags:
      --auto      Emit auto-upgrade (SnoozeNever) variant
  -h, --help      help for preamble
      --install   Write preamble to ~/.config/git-hop/skills/
      --never     Emit check-only (SnoozeAlways) variant

Global Flags:
      --config string   config file (default is $XDG_CONFIG_HOME/git-hop/config.json)
      --dry-run         preview changes without applying
      --force           bypass safety checks
      --format string   Output format (table, json, yaml) (default "table")
  -g, --global          use global hopspace in $GIT_HOP_DATA_HOME (default: local)
      --json            output in JSON format
      --no-color        Disable ANSI colour
      --no-hints        Suppress next-step hints after command output
      --porcelain       machine-readable output
  -q, --quiet           Suppress non-essential output
  -v, --verbose         verbose output
